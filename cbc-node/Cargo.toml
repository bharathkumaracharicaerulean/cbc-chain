# 									=======  CBC-NODE  ======

[package]
# Basic package metadata
name = "cbc-node"                     # Name of the package
description = "cbc-node."             # Short description of the package
version = "0.1.0"                     # Version of the package
license = "Unlicense"                 # License under which the package is distributed
authors.workspace = true              # Authors are inherited from the workspace
homepage.workspace = true             # Homepage URL is inherited from the workspace
repository.workspace = true           # Repository URL is inherited from the workspace
edition.workspace = true              # Rust edition is inherited from the workspace
publish = false                       # Prevent this package from being published to crates.io

# Build script
build = "build.rs"                    # Specifies the build script for this package

[package.metadata.docs.rs]
# Documentation settings for docs.rs
targets = ["x86_64-unknown-linux-gnu"] # Target platforms for which documentation is built

[dependencies]
# === CLI and Command-Line Parsing ===
clap = { features = ["derive"], workspace = true } # Command-line argument parsing library

# === Benchmarking ===
frame-benchmarking-cli.default-features = true    # CLI tools for runtime benchmarking
frame-benchmarking-cli.workspace = true          # Use workspace version

# === Metadata and Extensions ===
frame-metadata-hash-extension.default-features = true # Metadata hash extension
frame-metadata-hash-extension.workspace = true       # Use workspace version

# === FRAME System ===
frame-system.default-features = true            # FRAME system module
frame-system.workspace = true                   # Use workspace version

# === Async and Futures ===
futures = { features = ["thread-pool"], workspace = true } # Asynchronous programming utilities

# === JSON-RPC ===
jsonrpsee = { features = ["server"], workspace = true }    # JSON-RPC server implementation

# === Pallet Transaction Payment ===
pallet-transaction-payment-rpc.default-features = true # RPC for transaction payment pallet
pallet-transaction-payment-rpc.workspace = true       # Use workspace version
pallet-transaction-payment.default-features = true    # Transaction payment pallet
pallet-transaction-payment.workspace = true           # Use workspace version

# === Substrate Core Components ===
sc-basic-authorship.default-features = true # Basic authorship logic for block production
sc-basic-authorship.workspace = true        # Use workspace version
sc-cli.default-features = true              # Substrate CLI utilities
sc-cli.workspace = true                     # Use workspace version
sc-client-api.default-features = true       # Substrate client API
sc-client-api.workspace = true              # Use workspace version
sc-consensus-aura.default-features = true   # AURA consensus algorithm
sc-consensus-aura.workspace = true          # Use workspace version
sc-consensus-grandpa.default-features = true # GRANDPA consensus algorithm
sc-consensus-grandpa.workspace = true       # Use workspace version
sc-consensus.default-features = true        # General consensus utilities
sc-consensus.workspace = true               # Use workspace version
sc-executor.default-features = true         # Substrate executor
sc-executor.workspace = true                # Use workspace version
sc-network.default-features = true          # Substrate networking
sc-network.workspace = true                 # Use workspace version
sc-offchain.default-features = true         # Offchain worker utilities
sc-offchain.workspace = true                # Use workspace version
sc-service.default-features = true          # Substrate service utilities
sc-service.workspace = true                 # Use workspace version
sc-telemetry.default-features = true        # Telemetry support
sc-telemetry.workspace = true               # Use workspace version
sc-transaction-pool-api.default-features = true # Transaction pool API
sc-transaction-pool-api.workspace = true    # Use workspace version
sc-transaction-pool.default-features = true # Transaction pool implementation
sc-transaction-pool.workspace = true        # Use workspace version

# === Runtime ===
cbc-runtime.workspace = true                # CBC runtime

# === Substrate Primitives ===
sp-api.default-features = true              # Substrate API primitives
sp-api.workspace = true                     # Use workspace version
sp-block-builder.default-features = true    # Block builder primitives
sp-block-builder.workspace = true           # Use workspace version
sp-blockchain.default-features = true       # Blockchain primitives
sp-blockchain.workspace = true              # Use workspace version
sp-consensus-aura.default-features = true   # AURA consensus primitives
sp-consensus-aura.workspace = true          # Use workspace version
sp-core.default-features = true             # Core Substrate primitives
sp-core.workspace = true                    # Use workspace version
sp-genesis-builder.default-features = true  # Genesis block builder
sp-genesis-builder.workspace = true         # Use workspace version
sp-inherents.default-features = true        # Inherent data primitives
sp-inherents.workspace = true               # Use workspace version
sp-io.default-features = true               # Substrate I/O primitives
sp-io.workspace = true                      # Use workspace version
sp-keyring.default-features = true          # Keyring for testing and development
sp-keyring.workspace = true                 # Use workspace version
sp-runtime.default-features = true          # Runtime primitives
sp-runtime.workspace = true                 # Use workspace version
sp-timestamp.default-features = true        # Timestamp pallet primitives
sp-timestamp.workspace = true               # Use workspace version

# === RPC System ===
substrate-frame-rpc-system.default-features = true # FRAME RPC system
substrate-frame-rpc-system.workspace = true       # Use workspace version

[build-dependencies]
# === Build Script Utilities ===
substrate-build-script-utils.default-features = true # Utilities for build scripts
substrate-build-script-utils.workspace = true       # Use workspace version

[features]
# === Default Features ===
default = ["std"]                      # Default feature set includes standard library support
std = ["cbc-runtime/std"]              # Enable standard library support for the runtime

# === Runtime Benchmarking ===
runtime-benchmarks = [
    "frame-benchmarking-cli/runtime-benchmarks", # Enable runtime benchmarking for CLI
    "frame-system/runtime-benchmarks",          # Enable runtime benchmarking for FRAME system
    "pallet-transaction-payment/runtime-benchmarks", # Enable runtime benchmarking for transaction payment pallet
    "sc-service/runtime-benchmarks",            # Enable runtime benchmarking for Substrate service
    "cbc-runtime/runtime-benchmarks",           # Enable runtime benchmarking for CBC runtime
    "sp-runtime/runtime-benchmarks",            # Enable runtime benchmarking for runtime primitives
]

# === Try Runtime ===
try-runtime = [
    "frame-system/try-runtime",                 # Enable try-runtime for FRAME system
    "pallet-transaction-payment/try-runtime",   # Enable try-runtime for transaction payment pallet
    "cbc-runtime/try-runtime",                  # Enable try-runtime for CBC runtime
    "sp-runtime/try-runtime",                   # Enable try-runtime for runtime primitives
]
